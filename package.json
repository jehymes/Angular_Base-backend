{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend Json Server",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3001",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jehymeson Gil Alves Oliveira",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.3"
  }
}
